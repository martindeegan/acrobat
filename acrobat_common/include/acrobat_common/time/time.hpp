#include <chrono>

namespace acrobat {
// using milliseconds = std::chrono::;
// using seconds      = std::chrono::duration<double>;
// using seconds      = std::chrono::duration<double>;
// using seconds      = std::chrono::duration<double>;
// using seconds      = std::chrono::duration<double>;
} // namespace acrobat